#ifndef COMPUTER_GAME_INST_LIST_H
#define COMPUTER_GAME_INST_LIST_H
#include "main.h"
// Returns the y-value of where active element is drawn
int draw_inst_list(GameState state);
#endif //COMPUTER_GAME_INST_LIST_H